import React from 'react';
import {SearchLocation} from "pages/MainPage/SearchLocation";

const MainPage = () => {
    return (
        <div>
            <SearchLocation />
        </div>
    );
};

export default MainPage;